

const initState = {
    pageViews: 0
}

const CandidateReducer = (state = initState, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default CandidateReducer;